#!/bin/sh

# Name:
#   sh_esa_s1_gsar_expt_safe
#   

# Purpose:
#   

# Example:
#   

# Modifications:
#   

# Algorigthm:
#   

# Dependency:
#   


PROG=sh_esa_s1_gsar_expt_safe

while [ "$1" != "" ]; do
  case $1 in
    *)
      echo "[$PROG]ERROR: invalid option ($1)!!"
      echo "[$PROG]Usage: ${PROG} "
      echo "[$PROG]Usage: e.g.,"
      echo "[$PROG]Usage: ${PROG} "
      exit 1
      ;;
  esac
  shift 2  
done

oldpath=`pwd`

expts=`find . -maxdepth 1 -type d -name "[0-9]*-*"`
for expt in $expts; do
  cd $oldpath
  pwd
  echo expt $expt
  cd $expt
  pwd
  echo sh_esa_s1_expt_safe -path_safe /g7d/gsar/metainfo/manifest.safe/
  echo sh_esa_s1_expt_safe -path_safe /g7d/gsar/metainfo/manifest.safe/ | sh
  exit
done
